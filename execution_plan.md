# MVSingh Studio — Execution Plan

Single source of truth for the Studio initiative. Update this file whenever work completes, a requirement is discovered, or a decision is made. Do not maintain a parallel plan elsewhere.

Status legend: `[ ]` Not started · `[-]` In progress · `[x]` Completed · `[!]` Blocked · `[?]` Decision required

---

## Vision

MVSingh Studio is the business-facing half of the MVSingh ecosystem: a founder-led digital-building practice that digitalizes and automates businesses, starting in Punjab/India. It should read as "an engineer who understands business and can build the system end-to-end," not as a generic web agency. `mvsingh.in` (personal) and the Studio stay siblings in one ecosystem rather than merging into one agency site.

## Current State (last updated 2026-09-03)

- **Location**: this repo now lives at `/Users/vusic/Mvs/Studio` (top-level, sibling to `Brand/`), relocated from `Brand/Portfolio/scale-crafted-brand`. It is its own git repository (not part of a monorepo) — `Brand/Portfolio/` was just where it happened to be checked out locally. `Brand/Portfolio/init.md` (the original brief) stays where it is as reference documentation; it was not moved.
- **Stack**: Next.js 16 (App Router, Turbopack), React 18, TypeScript, Tailwind + shadcn/Radix primitives, Framer Motion (`motion`), Supabase (Postgres + Auth + Storage) as the only backend, deployed on Vercel. Single repo, git history intact, remote `mvsingh07/scale-crafted-brand`.
- **Live identity**: One CMS-driven "hub" ecosystem for **mvsingh** (`identity_profile`, `ecosystem_theme` tables), rendered at `/` as a single scrolling page (Home → About → Vision → Services → Blogs → Contact) with matching standalone routes (`/about`, `/services`, `/vision`, `/blogs`, `/contact`) for direct linking/SEO. Nav links, hero copy, and theme tokens are DB-driven and editable via `/forge` (password-gated CMS, Ctrl+Alt+L to reach from the hub).
- **Design language already in place**: near-black canvas (`#0A0A0A`), gold accent (`#C9A55A`/`#E0C27A`), Cinzel display serif + Inter body, generous motion (typewriter hero, scroll reveals, a disabled-but-present mandala/"sacred geometry" motif, a "DivineAura" component on the Vision page). This is premium and distinctive, but leans spiritual/mystical in places — fine for the personal site's Vision section, likely too much for a business studio.
- **Services content already written and seeded**: `docs/services.md` (a full "Punjab businesses in the AI era" services script) is already live in the `ecosystem_services` table and rendered at `/services` — Digital Presence, Search Visibility (SEO/GEO), AI & Automation, Custom Business Systems, Digital Business Tools, Mobile Applications. This is Studio-grade content sitting inside the personal site today. **Reuse it — don't rewrite from scratch.**
- **Legacy/parallel surfaces that predate the current CMS** (from an earlier "digital ecosystem platform" pivot documented in `docs/plan.md` / `docs/folder-structure.md`):
  - `/tech`, `/portfolio`, `/portfolio/tech` — three routes, all rendering the same `getProfileData("manvir")` output from the older `profiles` table. Redundant with each other and disconnected from the newer `identity_profile`/`ecosystem_*` tables.
  - `/home` — a second, separately-built full "hub" page, distinct from `/` and `/tech`. Unclear if still linked from anywhere live.
  - `admin/*` (dashboard, users, payment-config, revenue, transactions) + `supabase/functions/{create-subscription,razorpay-webhook}` + `transactions`/`admin_config`/`profiles` tables — scaffolding for a multi-tenant "digitalavatar.ai" SaaS (per-user portfolios with subscriptions) that Phase 3 of the old plan describes. This is **not** the Studio's client-facing business; it looks dormant.
  - `vercel.json` still has a `/(.*) → /index.html` SPA rewrite left over from the pre-Next.js Vite app. Next.js/Vercel doesn't need this — it's dead config, not actively harmful but should be removed when touching deploy config.
- **SEO baseline exists**: per-route metadata via `layout.tsx` files, a `sitemap.ts`, `robots.txt` blocking `/admin` and `/forge`, Person JSON-LD on the root layout. No per-service-page structured data, no business/Organization schema yet — expected, since there's no business entity yet.
- Next.js 16 renamed the middleware file/export from `middleware.ts` to `proxy.ts` (`src/proxy.ts`) — this repo already uses the new convention; it shows up in `next build` output as "Proxy (Middleware)".

## Strategic Decisions

| # | Decision | Reasoning | Status |
|---|---|---|---|
| 1 | Studio lives at **`studio.mvsingh.in`** (subdomain), same Vercel project, same Next.js app, routed via `middleware.ts` on `Host` header — not a path (`/studio`) and not a second Vercel project. | A subdomain is free (just a DNS CNAME + Vercel domain alias) and keeps one codebase/one deploy, which is what "same repository" implies. A path would blur the personal/business separation the brief explicitly wants; a second project would duplicate the design system, Supabase client, and CMS. | `[x]` Decided |
| 2 | "Move `scale-crafted-brand` into `Studio/`" means relocating the whole repo directory to a new top-level path (`Studio/`, sibling to `Brand/`) — **not** nesting it as a subfolder inside itself. Since the repo root already *is* `scale-crafted-brand` (package.json, `src/`, etc. all sit at repo root, and `Brand/Portfolio/` was never part of the git repo — just this machine's local checkout path), the move is a plain directory rename. It changes nothing inside the app: no import paths, no `tsconfig`/`tailwind` content globs, no Vercel "Root Directory" setting (Vercel deploys from the GitHub remote, which is unaffected by a local path change). | Initially assumed (see superseded reasoning below) this required internal restructuring like a monorepo migration. Confirmed by inspection that `Brand/Portfolio/` isn't a git boundary — it's just a folder on disk — so the correct move is far simpler and lower-risk than first modeled. | `[x]` Decided and executed |
| 3 | Legacy SaaS scaffolding (`/admin/*`, `profiles`/`transactions`/`admin_config` tables, Razorpay functions, `/tech`+`/portfolio`+`/portfolio/tech`+`/home` duplicate routes) is **archived in place, not deleted**, until confirmed dead. | Section 4 of the brief says don't blindly rebuild or destroy useful infrastructure, and never assume something is safe to delete without checking. Payment/revenue code touching real transactions is exactly the kind of thing that shouldn't be removed on a guess. | `[?]` Needs confirmation |
| 4 | Studio design language = **same palette and type system, less mysticism**: keep the black/gold/Cinzel identity (it already reads premium and technical), drop mandala/"DivineAura"-style motifs from Studio pages specifically. Those stay on the personal Vision page where they fit. | Brief #9 says evolve, don't replace, the existing identity; brief #25 explicitly warns against sounding "overly philosophical or spiritual" for the business message. | `[x]` Decided |
| 5 | Reuse the existing `ecosystem_services` content (already Studio-toned, per `docs/services.md`) as the seed for Studio's service sections, re-grouped under the brief's five pillars (Digital Presence, Business Systems, Automation, AI, Visibility) instead of rewriting. | The content already exists, is honest (no invented claims), and matches the brief's tone. Duplicating effort to rewrite it would be waste. | `[x]` Decided |
| 6 | Plan tiers renamed **Foundation / Growth / Transformation** (replacing Gold/Platinum/Diamond). Names echo the vocabulary already used in the Business Transformation section, reinforcing one consistent story instead of introducing a separate metaphor. | Original names read as jewelry/wedding-package tiers, cutting against brief #16's "avoid looking like a cheap package-based web agency." Open to change if the founder prefers different names. | `[x]` Decided (open to revision) |
| 7 | Years-of-experience claim: confirmed by the user as **4 years**. The live About section on the personal site still says "3+ years building" — that's now stale and should be reconciled (tracked in Milestone 7). | Brief #32: never invent numbers; user gave the authoritative figure directly. | `[x]` Decided |
| 8 | Company is **not registered** — no legal entity, no GST, no trademark filed. Studio operates as an individual/founder for now. All Studio copy must avoid implying a registered company, "team," trademark (™/®), or legal entity until this changes. Registration + trademark filing are tracked as explicit Phase 3 tasks, not blockers for the Phase 1 website. | User confirmed directly; brief #28 says don't block the website on this, but also never claim what isn't true (#32). | `[x]` Decided |
| 9 | **Challenging brief #20/Milestone 4**: don't create a Google Business Profile yet — Google's guidelines exclude businesses that only serve customers virtually with no staffed physical location and no in-person contact, and a fully-remote solo practice (today's reality) fits that exclusion exactly. **Alternative**: if the founder is willing to occasionally meet local Punjab clients in person, the Studio qualifies as a Service-Area Business (no public address required) and a listing is genuinely worth having for local search. **Tradeoff**: creating one anyway risks suspension and can taint the business name/phone for a future legitimate attempt; waiting costs only some early local-search visibility, which SEO/GEO work (Milestone 3) already covers in the meantime. **Recommendation**: hold off until the founder confirms which path applies — documented as a guide either way (`studio-documents/03-google-business-profile-setup.md`) so it's a one-step action once decided. | Brief #31: obligated to flag a strategic disagreement rather than silently comply or silently skip. | `[?]` Needs founder's answer |
| 10 | Social platform shortlist: build Instagram + a linked Facebook Page now; use the founder's existing personal LinkedIn for Studio content instead of a new Company Page; defer YouTube and X. | Brief #21 says "only recommend platforms that serve the strategy" — a new LinkedIn Page starts at zero audience and adds upkeep for no near-term benefit over posting from the founder's already-connected personal profile; Facebook has real overlap with Punjab/India SMB owners (the actual client base) at near-zero extra cost via IG cross-posting; YouTube/X don't clearly serve this audience yet. | `[x]` Decided (open to revision) |
| 11 | A second, separate brief (`init.md`, "Final Landing Page Narrative & Content Integration") asked for a full narrative/emotional overhaul of the `/studio` page — NOT a redesign. Scoped to 3 new components (Heritage, Digital Future, Culture+Tech) plus enhancements to existing sections, rather than the brief's own literal ~16-section suggested flow, per its own "existing design + stronger story, not +15 sections" rule. | The brief itself explicitly authorizes/expects scoping judgment here (its own section 34). A Plan-agent review caught two placement issues before implementation: "give humans their time back" had no assigned home (folded into Approach) and "the future is arriving quietly" was initially merged mid-page but the brief explicitly calls it a closing idea (relocated to a new closing passage before Contact). | `[x]` Decided and executed |
| 12 | No real photography exists for the sections that want it (Heritage, Culture+Tech). Built a placeholder-fill fallback (`AnnotatedImageReveal`'s now-optional `src`, and a new `ImagePlaceholder` primitive) rather than generating or sourcing stand-in images. | User's explicit choice when this was raised: placeholders only, no invented photos, no image generation. | `[x]` Decided |
| 13 | `StudioTransformation` restructured from a flat 7-stage list to the brief's 5-layer model (Digital Presence / Business Organization / Automation / Intelligence / Future Readiness), each with 3-5 sub-item chips — net fewer nodes than before. | Brief section 10 calls this framing "one of the strongest visual sections on the site" with concrete sub-items given; a straight relabel would have undersold it, and 5 denser nodes reads shorter than the old 7. | `[x]` Decided and executed |
| 14 | `StudioServices`' live Supabase `ecosystem_services` data and card rendering were left untouched; the brief's "Understand → Digitize → Organize → Automate → Evolve" pipeline framing was added as intro copy only, not a data re-migration. | Narrows Decision #5 ("re-group under 5 pillars") — re-migrating live CMS content for a copy-alignment pass was judged disproportionate risk versus a framing paragraph achieving the same "rewrite/reposition, don't duplicate" requirement (brief section 34). | `[x]` Decided (narrows Decision #5) |
| 15 | "Impact Program" / "Guidance Program" renamed to **MV Impact** / **MV Guidance**. | Brief sections 20-21 give these names directly (not "propose one if you find a better one" — that's the brief's own hedge for if they're disliked). No naming exploration needed. | `[x]` Decided |
| 16 | Hero headline changed to Direction C: "Preserve what makes you unique. Build for what comes next." (brief offered 3 directions and asked for a judgment call on which fits the existing design). | Fits the existing italic-serif "story" motif better than the other two options, and sets up the later Culture+Tech section's payoff ("identity travels further"). | `[x]` Decided |

## Open Questions

- `[?]` **Heritage image** — `IMAGE REQUIRED` (per `init.md` section 26's own mandatory format): **Purpose**: ground the "India, Built by People" chapter in a real place instead of a placeholder. **Composition**: a cinematic Indian local market/business environment — traditional shopfronts, craftsmen, and customers coexisting naturally; folds in the brief's separate "Punjab → India" image idea (section 26, Image 05) rather than using both. **Style**: premium editorial realism, no text, wide cinematic composition — not a tourism-brochure look. **Aspect ratio**: matches `AnnotatedImageReveal`'s full-bleed block, roughly 16:9 to 21:9 depending on final height. **Placement**: top of `StudioHeritage` (chapter 01), replacing the current placeholder fill. Only this one image is scoped in — the brief's other 4 suggested image concepts (Digital Transition, AI Future, Human+Automation) weren't needed by the 3-component plan actually implemented and shouldn't be forced in later just because they exist in the brief.
- `[?]` **Culture + Technology image** — same mandatory format: **Purpose**: reassure that digitalization doesn't erase identity. **Composition**: a traditional craft/business detail (workshop, restaurant kitchen, textile work) shown alongside its modern digital equivalent (a phone/laptop screen, subtly). **Style**: warm, human, editorial — not a stock "hands typing" photo. **Aspect ratio**: roughly 4:3 to 1:1 (half-width slot). **Placement**: `StudioCultureTech`'s image half, replacing the current `ImagePlaceholder`.
- `[x]` **Repo restructuring** — resolved: repo relocated from `Brand/Portfolio/scale-crafted-brand` to top-level `Studio/`. See Decision #2 and Change Log.
- `[?]` **DNS + Vercel domain for `studio.mvsingh.in`** — account-level access I don't have. Step-by-step guide written: `studio-documents/01-dns-and-domain-setup.md`; verify with `studio-documents/02-post-launch-checklist.md` once it resolves.
- `[x]` **Legacy SaaS scaffolding** — resolved: leave untouched in Phase 1 (`/admin/*`, Razorpay functions, `profiles`/`transactions`/`admin_config`). Not confirmed dead; not touched.
- `[x]` **`/home`, `/tech`, `/portfolio`, `/portfolio/tech` duplicate routes** — resolved: consolidated to `/portfolio` only. `/tech` was already 308-redirected to `/portfolio` by `src/proxy.ts`; added the same 308 redirect for `/portfolio/tech` → `/portfolio` and `/home` → `/`. Deleted the now-dead route files (`src/app/tech/`, `src/app/portfolio/tech/`, `src/app/home/`) and the orphaned `TechContent` component. Verified with `npm run build`. Sitemap trimmed to match. Not yet committed.
- `[x]` Plan-tier naming — resolved: Foundation / Growth / Transformation (see Decision #6). Open to revision if the founder wants different names.
- `[?]` **Google Business Profile eligibility** — needs the founder's answer: does client work ever happen in person (even occasionally), or is it 100% remote? Google's own policy excludes fully-virtual businesses with no physical/staffed location and no in-person customer contact from having a Business Profile at all — this isn't a formality, a listing created without qualifying risks suspension. See Decision #9 and `studio-documents/03-google-business-profile-setup.md`.

## Decisions Log

- 2026-09-03 — Studio ships as a subdomain (`studio.mvsingh.in`) inside the existing Next.js app via host-based middleware, not a new app or a path segment. (Decision #1)
- 2026-09-03 — Repository relocated from `Brand/Portfolio/scale-crafted-brand` to top-level `Studio/` via a plain directory rename (`.git` and all history intact); this was simpler than originally modeled since `Brand/Portfolio/` was never a git boundary. (Decision #2)
- 2026-09-03 — Existing `ecosystem_services` CMS content is the source of truth for Studio's "What We Build" section; not rewritten from scratch. (Decision #5)
- 2026-09-03 — Plan tiers renamed Foundation / Growth / Transformation. (Decision #6)
- 2026-09-03 — Confirmed by founder: 4 years of professional experience; company not yet registered, no GST, no trademark filed. (Decisions #7, #8)

## Change Log

- 2026-09-03 — Repository audit completed. This file created. No application code changed yet.
- 2026-09-03 — Duplicate routes consolidated to `/portfolio`: deleted `src/app/tech/`, `src/app/portfolio/tech/`, `src/app/home/`, and the orphaned `TechContent` component; added 308 redirects in `src/proxy.ts` for `/portfolio/tech → /portfolio` and `/home → /`; trimmed `src/app/sitemap.ts`. Build verified green. Staged, not committed.
- 2026-09-03 — Repo relocated: `Brand/Portfolio/scale-crafted-brand` → `Studio/` (top-level). Verified `git status`/`git log`/`git remote` and `npm run build` all intact post-move. Deleted the dead `vercel.json` SPA rewrite (leftover from the pre-Next.js Vite app; Vercel auto-detects Next.js and needs no rewrite config). This `execution_plan.md` moved from a nested `Studio/Studio/` path up to the new repo root. All of the above is staged, not committed.
- 2026-09-03 — Milestone 2 built: `/studio` route (layout + page), 10 new `src/components/studio/*` components (nav, hero, problem, services, transformation, approach, experience, plans, contact, footer, shared tokens), host-based rewrite in `src/proxy.ts` for `studio.mvsingh.in`. Build and dev-server smoke test both green. Staged, not committed. (Incidental: a stray copy of the reference brief `init.md` appeared inside this repo mid-session and the original at `Brand/Portfolio/init.md` was gone — moved it back to `Brand/Portfolio/init.md`; cause unclear, worth a sanity check next session that it's still there.)
- 2026-09-03 — Creative + mobile pass on the Studio page: reframed it as an 8-chapter story with a scroll-linked connecting thread and two narrative transition lines; fixed a mobile-only pill-wrapping issue in the Transformation stepper; enlarged the nav's mobile tap target. Verified with Playwright (installed to the scratchpad dir for this check, not added as a project dependency) across desktop (1440px) and mobile (375px) widths, in both light and dark theme — no layout overflow, no console errors, live Supabase data renders correctly, mobile menu works. Also wrote `studio-documents/` (README, DNS/domain setup guide, post-launch checklist) covering the manual account-level steps to put `studio.mvsingh.in` live. All staged, not committed.
- 2026-09-04 — Hero video slowed to 0.7x playback (`videoRef.current.playbackRate`, since there's no declarative attribute for it), made full-bleed (edge to edge, height `clamp(420px, 88vh, 940px)`, no card/border/rounding), with a flat darkening overlay on top. Services cards ("What We Build") now slide in individually from alternating left/right as each one crosses into view while scrolling, instead of all fading in together. **Bug caught and fixed in the same pass**: the pre-animation off-screen `x` offset on a full-width mobile card (single-column grid) pushed it past the viewport edge and created a real horizontal scrollbar even though it wasn't visually obvious — confirmed via `document.documentElement.scrollWidth` (447 vs 375 clientWidth), not just eyeballing. Fixed by gating the side-slide to `≥1024px` (`useIsDesktop`, a `matchMedia` hook) and falling back to a plain vertical fade below that, since a single mobile column doesn't have "sides" to slide in from anyway. Re-verified scrollWidth === clientWidth on mobile after the fix, and video `playbackRate`/full-bleed width/no console errors on desktop. Staged, not committed.
- 2026-09-04 — Hero restructured on user request: the video is no longer a background layer with text/scrim overlaid on it. It's now its own contained showcase block (rounded card, padded clear of the sticky nav above it, nothing drawn on top of the footage), and the headline/CTAs/floating tags moved to a separate copy block below it on the normal page background. Verified with Playwright at 1600px and 375px — no overlap between nav and video at rest or after a scroll, no overflow, no console errors.
- 2026-09-04 — Hero background swapped to the user-provided `public/video/studio-bg.mp4` (1920×1080, ~8s loop, ~10MB — a "digital transformation" branded motion graphic with its own baked-in on-screen text/icons). Added a two-layer scrim (radial vignette + bottom fade to solid `--bg-primary`) so the existing headline stays legible and the video blends into the next section instead of cutting off with a hard edge; `autoPlay muted loop playsInline` matching the personal hub's existing hero-video pattern (`src/app/page.tsx`). Verified with Playwright: video actually plays (advancing `currentTime`, not paused) on both a 1600px and a 375px viewport, no console errors, no overflow. Staged, not committed.
- 2026-09-03 — Visual overhaul on user request ("too simple," referencing penguin-capital.co.jp/en for direction). Inspected the reference site (Playwright screenshots, not copied) and adapted its editorial-finance language — not its literal content — into the existing gold/black identity: added a scoped Fraunces italic display serif (`studio/layout.tsx`, CSS var `--font-fraunces`) for large numerals and accent lines; added `StudioBackdrop` (fixed vertical rule grid + drifting glow + film grain) as the page's base texture; added `ChromeOrb` (a CSS-only glossy sphere, standing in for the reference's rendered 3D objects — no new 3D/asset pipeline needed) and `GhostNumeral` (large outline-only numerals) as reusable primitives in `shared.tsx`; rebuilt `ChapterEyebrow` as a bracket label paired with a mirrored large numeral. Applied these across Services (tilted cards, corner number ribbons, chrome-orb icons), Approach (chrome-orb numbered badges), Plans (tilted cards, chrome-orb numerals), Transformation (chrome-orb stage markers, giant ghost "07" watermark, gentle left/right drift per stage), and Hero (four floating mouse-parallax collage tags naming the Approach steps, plus a giant ghost "MVS" watermark). Re-verified with Playwright at 1440px/375px, dark mode — no console errors, no mobile overflow, corner ribbons and chrome orbs render correctly on both. Staged, not committed.
- 2026-09-04 — Fixed a hero video crop bug: on wide/short viewports (common maximized-but-not-full-height browser windows), `object-fit: cover` was cropping the video symmetrically top and bottom, and the footage's own on-screen text/UI sits close to the top of the frame — so the top got clipped. Extracted 10 frames across the loop with a small AVFoundation/Swift script (no ffmpeg installed) to confirm where the footage's content actually sits, then set `objectPosition: "center top"` so any forced vertical crop comes entirely from the bottom (desk clutter) instead of the top (headline/UI). Verified via Playwright at an exaggerated 1600×700 window, 1440×900, and 375×812 — `objectPosition` confirmed `"50% 0%"` at all three, screenshot at 1600×700 shows the full top edge intact.
- 2026-09-04 — Built `src/components/studio/AnnotatedImageReveal.tsx`: a reusable component recreating studio-bg.mp4's own motion language (extracted and studied via the same frame-by-frame method above) for a static image — the footage never moves the camera, it's one fixed shot where a headline builds line-by-line, a labeled feature list fades in item-by-item with small connector ticks, and a closing line lands last, all cumulative rather than replacing each other. The component reproduces that as a scroll-triggered (`whileInView`, once) staggered reveal over any `src` image, using the site's existing tokens (`ChromeOrb`, `FONT_H`/`FONT_ED`, `GOLD`) so it matches the rest of the page. Not wired into the live page yet — no target image or copy has been supplied; ready to drop in once one is. Type-checked clean (`tsc --noEmit`, no new errors).
- 2026-09-04 — Milestone 3 (SEO & GEO) built: `ProfessionalService` JSON-LD for Studio (linked to the existing Person JSON-LD via `founder`, deliberately excluding anything implying registration/address/team/pricing per Decision #8); a real OG image generated via `next/og` (`src/app/studio/opengraph-image.tsx`, no external tool, no invented visuals — just the site's own copy/palette rendered as an image) plus a `metadataBase` scoped to `studio.mvsingh.in`; `src/app/studio/sitemap.ts` (nested per-segment sitemap, a Next.js-supported pattern) so `studio.mvsingh.in/sitemap.xml` resolves correctly through the existing host rewrite. **Bug found and fixed along the way**: `studio.mvsingh.in/robots.txt` was 404ing (confirmed by build output + a real request against `Host: studio.localhost:3000`) because the generic proxy rewrite sent it to `/studio/robots.txt`, which doesn't exist — I'd first tried a nested `app/studio/robots.ts` assuming it worked like the nested sitemap, but Next.js only supports the root-level `robots.ts` convention and silently generated no route for it. Deleted that dead file and instead special-cased `/robots.txt` directly in `proxy.ts` to serve the correct content (with its own `Sitemap:` line) before the generic rewrite runs. Verified end-to-end against a production build (`npm run build && npm run start`) with `Host: studio.localhost:3000`: robots.txt, sitemap.xml, the OG image (200, `image/png`), and both JSON-LD blocks all confirmed correct via direct `curl`; `mvsingh.in/robots.txt` confirmed unaffected. Content pass for AI/GEO discoverability: audited existing copy against brief #19's what/who/where/why bar — already meets it, no rewrite needed. Staged, not committed.
- 2026-09-04 — Milestone 4 (Google Business Profile): challenged the brief's assumption that a listing should just be created — Google's own policy excludes fully-virtual businesses with no physical/staffed location and no in-person customer contact, which is what the Studio is today. Recorded as Decision #9 (issue/alternative/tradeoff/recommendation per brief #31) and as an open question for the founder (Service-Area Business path if in-person contact ever happens, vs. skip for now). Wrote `studio-documents/03-google-business-profile-setup.md` covering both paths so it's actionable either way once decided.
- 2026-09-04 — Milestone 7 (Personal Website Refinement) partially built: fixed the stale "3+ years" stat to "4+" in `AboutSection.tsx` (the live-reachable copy; a second hardcoded "3+ years" in the unreachable legacy `About.tsx` was left alone, consistent with Decision #3's precedent for untouched legacy scaffolding); added a non-destructive callout on `/services` pointing business inquiries to `studio.mvsingh.in` instead of removing the existing (still-live, CMS-driven) service content; added a fixed "Studio ↗" outbound link to `Navbar.tsx`, on both desktop and the mobile menu. Confirmed via a real production build + Playwright (1440px and 375px, dark mode) that nav links are actually populated live from Supabase (`identity_profile.nav_links`, not the hardcoded fallback array — verified by seeing "Portfolio"/"Brand" render, which aren't in the fallback at all), which is why the Studio link was added as a fixed item alongside the CMS-driven links rather than by editing the code fallback. No overflow, no console errors on either viewport. Deeper `/services` content trimming left as an open call for the founder rather than done unilaterally. Staged, not committed.
- 2026-09-04 — Milestone 5 (Social Presence) written up: checked all three of the brief's suggested Instagram handles via web search (no existing account found for any three, but flagged that Instagram blocks reliable unauthenticated verification — the brief's own "do not assume availability" instruction — so the founder should do the instant in-app check before finalizing). Recommended `@mvsingh.studio`, full profile setup, 5 narrowed content pillars (deferring Case Studies/Products until they're unblocked by Milestones 6/Phase 4), a 5-post launch sequence, and a platform shortlist (Decision #10: Instagram + Facebook now, founder's existing personal LinkedIn instead of a new Company Page, YouTube/X deferred). `studio-documents/04-social-presence-strategy.md`. No code changed — this milestone is pure strategy/documentation, same as the DNS and GBP guides.
- 2026-09-04 — A second, separate brief appeared mid-session (`init.md`, "Final Landing Page Narrative & Content Integration") asking for a narrative/emotional overhaul of `/studio`, not a redesign. Audited the existing page against it, used a Plan-agent pass to pressure-test the initial scoping (it caught two real placement issues, see Decision #11), and got explicit user sign-off via plan mode before writing any code — this was a large enough change (touches nearly every section, adds new ones) to warrant that rather than proceeding ad hoc. Implemented Milestone 8 (see above) across 3 new components, 6 enhanced existing ones, a new closing mission passage, and a full chapter renumbering (Decisions #11-16). Along the way, found and fixed a real pre-existing hydration bug in `StoryThread` (see Milestone 8) that would have affected every real visitor browsing with OS-level reduced motion enabled — never caught in any prior verification pass because none had tested with that setting. Verified with a production build (`npm run build && npm run start`) and Playwright across desktop/mobile, light/dark, and a dedicated reduced-motion pass — all clean, zero console errors, zero layout overflow. Two `IMAGE REQUIRED` specs logged in Open Questions (Heritage, Culture+Technology); no images generated or sourced, per the user's explicit "placeholders only" decision — `AnnotatedImageReveal` and a new `ImagePlaceholder` primitive render a clean pending-photo state instead. Staged, not committed.
- 2026-09-04 — Hero background swapped again, to the user-provided `public/video/studio-bg-video.mp4` (1920×1080, 8s loop — an ambient sunset-to-dusk time-lapse over a Punjab/India skyline with device mockups showing MV Studios branding, birds crossing frame; no fast UI cuts like the previous clip). Studied it the same way as the prior clip (10 extracted frames via the AVFoundation/Swift script) before touching anything. Made the hero genuinely full-bleed on request ("utilize full space, no gap from anywhere"): removed the intentional top padding against the sticky nav (video now sits flush against it — confirmed 0px gap via Playwright bounding boxes) and grew the video box from `clamp(420px, 88vh, 940px)` to `clamp(520px, 96svh, 1080px)`. Recentered `objectPosition` to `center center` (the old `center top` was tailored to the previous clip's top-anchored on-screen text; this footage's subject sits in the vertical middle band) and dropped the old 0.7x `playbackRate` slowdown, since this footage is already slow-paced at native speed. Added new animations matching the new footage's own character instead of reusing the old clip's "staged UI reveal" language (that pattern lives on in `AnnotatedImageReveal`, built for the old clip): a slow infinite Ken Burns scale drift on the video (`1 → 1.045 → 1`, 26s loop), a warm gradient pooling at the base echoing the footage's golden-hour light, a "coming into focus" blur-to-sharp entrance on all the copy (replacing the plain fade), and a slow gold sheen sweeping once across the headline gradient text. All three infinite/looping effects are gated off under reduced motion. **Bug found and fixed in the same pass**: the reduced-motion gating for the new sheen effect and, on investigation, a *pre-existing* bug in `AnnotatedImageReveal.tsx` (`initial={reduce ? "visible" : "hidden"}`) both branched directly on `useReducedMotion()`'s return value at render time — the same SSR-vs-first-client-render mismatch class as the `StoryThread` bug fixed under Milestone 8, since that hook is unconditionally `false` during SSR but reads the real value synchronously on the client's very first render. Caught via a dedicated `reducedMotion: 'reduce'` Playwright pass (dev mode, full non-minified hydration diff) before it shipped; fixed both with the same mount-gated `useState`+`useEffect` pattern. Re-verified clean: zero console errors and zero horizontal overflow across desktop/mobile × light/dark × reduced-motion, full-page scroll-through. Production build green. Staged, not committed.
- 2026-09-04 — Two follow-up corrections to the hero, on user feedback: (1) shrinking the video box after the change above (to `clamp(400px, 60vh, 720px)`) had pushed the headline entirely below the fold — a plain 900px-tall viewport was consumed almost fully by nav+video alone, so the page looked text-less on load; caught with real bounding-box measurements (`h1` rect at `y: 749`+ on a 900px viewport), not just the earlier "no console errors" pass. (2) User then asked for the copy to sit *on top of* the video (the studio-bg.mp4-style cumulative build-up `AnnotatedImageReveal` was designed around) rather than below it, with the video restored to a genuinely full/complete frame. Fixed both together: moved the entire text block (eyebrow, headline, subtitle, CTAs, the four callout tags) inside the video's own box as an absolutely-positioned overlay, restored the box to `clamp(560px, 94svh, 1080px)`, added a stronger two-layer scrim (flat darken + a centered radial vignette) so the overlaid text stays legible against the photographic footage, and gave the four floating callout tags a connector-tick detail (borrowed directly from `AnnotatedImageReveal`'s `Callout` component) plus a cumulative one-by-one stagger instead of appearing all at once. **Bug found and fixed in the same pass**: two of the four tags used a negative `top` percentage (a leftover from when they were positioned inside the copy block *below* the video, where negative offset just meant empty space above the headline) — now that they're positioned relative to the video frame itself, a negative offset put them behind the sticky nav bar, making them invisible. Caught by getting real bounding-box coordinates for each tag's label (not just eyeballing a screenshot, which didn't make the missing tags obvious at a glance) and seeing two of them sitting inside the nav's own `y: 0–70` band. Fixed by moving both to a small positive offset. Re-verified all four tags visible and clear of the nav, and a full regression pass (desktop/mobile × light/dark × reduced-motion, zero console errors, zero overflow) plus a clean production build. Staged, not committed.

---

# Phase 1 — Foundation & Launch

## Milestone 1 — Repository & Architecture
- [x] Confirm working tree is clean (SEO edits committed by user as `96b9826`)
- [x] Decide final disposition of legacy SaaS scaffolding — left untouched (see Decision #3)
- [x] Relocate the repo to `Studio/` (top-level directory rename; git history and remote preserved — see Decision #2). No internal move of code was needed since the app already lived at repo root.
- [x] `tsconfig.json` / `tailwind.config.ts` content globs — unaffected, no change needed (relative paths only, nothing hardcoded to the old absolute location)
- [x] Vercel project settings — unaffected, no change needed (Vercel deploys from the GitHub remote, not this local path; Root Directory was and remains ".")
- [ ] Add host-based `proxy.ts` routing `studio.mvsingh.in` → Studio routes, `mvsingh.in` → personal routes — deferred to Milestone 2 kickoff, since there's no Studio content yet to route to
- [x] Verify `npm run build` succeeds and no broken imports remain (verified twice: after route consolidation, and again after the relocation)
- [x] Remove dead `vercel.json` SPA rewrite left over from the pre-Next.js app
- `[?]` DNS + Vercel domain alias for `studio.mvsingh.in` — manual, account-level task for the user (see Open Questions)

## Milestone 2 — Studio Website
- [x] Add host-based `proxy.ts` rewrite: `studio.mvsingh.in` (and `studio.localhost:3000` for local dev) → `/studio/*` transparently, so the subdomain shows clean URLs once DNS is configured. `mvsingh.in/studio` also works directly today, for preview before DNS exists.
- [x] Lock information architecture: Hero → Problem → Why This Matters Now → What We Build → Business Transformation → Our Approach → Experience → Plans (+ Impact/Guidance teasers) → Contact — built as one scrolling page at `/studio`, matching the personal hub's existing pattern.
- [x] Build Studio-specific nav/footer (`StudioNav`, `StudioFooter`) — same black/gold/Cinzel+Inter tokens as the rest of the ecosystem, no mandala/DivineAura motifs, links back to `mvsingh.in`.
- [x] "What We Build" (`StudioServices`) fetches the live `ecosystem_services` table — same 6 categories seeded from `docs/services.md`, not rewritten.
- [x] Business Transformation signature section (`StudioTransformation`) — Offline/fragmented → Digital presence → Organized data → CMS/CRM → Automation → AI-ready → Better decisions, as a vertical stepper.
- [x] Our Approach (`StudioApproach`) — Understand → Discover → Design → Build → Automate → Launch & Improve, with the brief's "we start with the problem" framing.
- [x] Experience section (`StudioExperience`) — states the confirmed 4 years; explicitly does not claim a "team" or invented credentials; mentions collaborators only in the brief's own suggested framing (general capability, not specific claims).
- [x] Plans section (`StudioPlans`) — Foundation / Growth / Transformation tiers, plus Impact Program and Guidance Program teaser cards (no eligibility logic yet — that's Phase 2/3).
- [x] Contact (`StudioContact`) — reuses `contact_submissions` (source: `"studio"`), same insert pattern as the personal site's contact form, `hello@mvsingh.in` as the direct-contact address.
- [x] Verified: `npm run build` clean, dev server smoke-tested (`/studio` renders hero/services copy; `Host: studio.mvsingh.in` correctly rewrites to the same content; no server errors in logs).
- [ ] Case studies / featured work — intentionally not built here; tracked as Milestone 6 (needs real projects, no invented results)
- `[?]` DNS + Vercel domain alias for `studio.mvsingh.in` still pending (manual, account-level — see Open Questions and `studio-documents/`)
- [x] **Creative pass**: reframed the whole page as one story told in 8 numbered chapters (bookended by "Every business has a story. Most of it still lives offline." in the hero and "...Let's write yours." in the closing contact section), added a scroll-linked gold "thread" rail (desktop only, `StoryThread` in `shared.tsx`) connecting the chapters, and two short italic connecting lines (`StoryBeat`) for pacing between sections.
- [x] **Mobile audit**: verified with Playwright at 375px width (iPhone-size) in both light and dark themes — no horizontal overflow, mobile hamburger menu opens/closes correctly, no clipped text; tightened the Transformation stepper's pill radius (was a full capsule that looked broken on 2-line wraps) and enlarged the nav's mobile menu tap target. Also verified at 1440px desktop in both themes — service cards, stepper, plans, and contact form all render correctly with live Supabase data and zero console errors.
- [x] Wrote `studio-documents/` (README + DNS/domain setup guide + post-launch checklist) — the manual, account-level actions needed to actually put `studio.mvsingh.in` live.

## Milestone 3 — SEO & GEO
- [x] `ProfessionalService` JSON-LD for Studio (`src/app/studio/layout.tsx`) — separate from the root's Person JSON-LD, links back to it via `founder`. Deliberately omits `legalName`, `address`, `employee`/`numberOfEmployees`, and any `aggregateRating`/`priceRange` — none of that is true yet (Decision #8), and none of it is invented here.
- [x] OG image for Studio (`src/app/studio/opengraph-image.tsx`, generated via `next/og` `ImageResponse` — no external design tool needed) + `metadataBase` scoped to `https://studio.mvsingh.in` so it and other auto-derived asset URLs resolve to the right host instead of inheriting the root layout's `mvsingh.in`. Per-page metadata was already in place from Milestone 2 (Studio is one scrolling page, so "per-page" is this one route); brief #30 explicitly says not to build out separate service pages ahead of search-intent justification, so that stays a documented future recommendation, not Phase 1 work.
- [x] `sitemap.ts` extended: added `src/app/studio/sitemap.ts` — Next.js supports nested per-segment sitemaps, so this generates at `/studio/sitemap.xml`, which `studio.mvsingh.in/sitemap.xml` already resolves to via the existing host rewrite in `proxy.ts`.
- [x] `robots.txt` extended for the `studio.` host — **not** via a nested `app/studio/robots.ts` (tried it first; confirmed by inspecting the build output that Next.js only supports the root-level `robots.ts` convention, silently generates no route for a nested one — deleted that dead file). Fixed properly in `proxy.ts`: `studio.mvsingh.in/robots.txt` is now served directly from the proxy with its own `Sitemap:` line pointing at the studio sitemap, before the generic host-rewrite rule would otherwise send it to a nonexistent `/studio/robots.txt` and 404. `mvsingh.in/robots.txt` is untouched.
- [x] Content pass for AI/GEO discoverability (brief #19's what/who/where/why) — audited existing copy rather than rewriting: Hero states where ("Punjab → India → Beyond"), Problem states what problems are solved (10 concrete pain points), Experience states why credible (4 years, direct hands-on work, no invented team/credentials) and where again ("companies across India"), Services/Plans state what's offered. Already meets the brief's bar without keyword-stuffing; no rewrite needed. New JSON-LD's `serviceType`/`areaServed` reinforce the same facts structurally.

## Milestone 4 — Google Business Profile
- [x] Confirmed eligibility is genuinely in question, not a formality — see Decision #9. Wrote `studio-documents/03-google-business-profile-setup.md` covering both paths (Service-Area Business if in-person contact ever happens, vs. skip entirely if fully remote) and the actual setup steps for the qualifying path. `[?]` Which path applies is the founder's call — tracked in Open Questions.

## Milestone 5 — Social Presence
- [x] Evaluated Instagram handle options (checked all three brief-suggested handles via web search — no existing account found for any, though Instagram blocks reliable unauthenticated verification; flagged that the founder should do the instant in-app check before finalizing), recommended `@mvsingh.studio` as primary, full profile setup (bio/photo/category/link/highlights), narrowed content pillars (5 of the brief's 10, deferring Case Studies and Products until Milestones 6/Phase 4 unblock them), and a 5-post launch sequence. Platform shortlist: Instagram + Facebook Page now; LinkedIn via the founder's existing personal profile rather than a new empty Company Page; YouTube and X deferred (see Decision #10). Written up in `studio-documents/04-social-presence-strategy.md`.

## Milestone 6 — Portfolio & Proof
- [ ] Select real projects, write Problem→Thinking→Solution→System→Result case studies (no invented results)

## Milestone 7 — Personal Website Refinement
- [x] Reconciled "3+ years building" → "4+" in `AboutSection.tsx`'s `DEFAULT_STATS` (the value actually reachable from live routes; the stats card block that renders it is currently commented out in JSX, so this wasn't visibly wrong today, but the source value was stale for whenever it's re-enabled or the Supabase `about_stats` override is empty). A second hardcoded "3+ years" string exists in `src/components/site/About.tsx`, but that component isn't imported from any `src/app/**` route — it's unreachable legacy scaffolding (`src/views/*` era), left untouched per Decision #3's precedent rather than assumed safe to edit or delete.
- [x] Added a callout on `/services` (`ServicesSection.tsx`'s `HeroBand`) pointing business inquiries to `studio.mvsingh.in`, rather than removing the existing content — the page's dynamic service cards and featured projects stay as portfolio/proof-of-work context for this personal site. A deeper trim (shortening or removing the full duplicated service grid) is a bigger content call affecting live public copy; flagged here rather than done unilaterally — happy to go further if wanted.
- [x] Added "Studio ↗" as a fixed outbound nav link (desktop + mobile menu) in `Navbar.tsx`, pointing to `studio.mvsingh.in`. Added as a static item alongside the CMS-driven `links` array rather than editing `FALLBACK_LINKS` — confirmed via the live rendered page that nav links are actually populated dynamically from Supabase `identity_profile.nav_links` (renders Portfolio/Brand, which aren't in the hardcoded fallback at all), so editing the code fallback alone wouldn't have changed the live nav. Mirrors `StudioNav.tsx`'s existing "Personal Site ↗" link back the other way.
- [x] Consolidate or redirect the legacy `/home`, `/tech`, `/portfolio`, `/portfolio/tech` duplicates — done ahead of schedule, see Change Log

## Milestone 8 — Narrative & Positioning Overhaul
*(New — from a second brief, `init.md`, delivered mid-session after Milestone 5. Full plan in Decisions #11-16 above; see Change Log for the implementation summary.)*
- [x] 3 new chapters built: `StudioHeritage` (01, "India, Built by People"), `StudioDigitalFuture` (07, "Becoming Understandable" — social-presence insight + hedged AI-agent framing + SEO/GEO ladder), `StudioCultureTech` (09, "Old values. New tools.")
- [x] Existing chapters enhanced in place (not duplicated): Hero headline, Problem's "The Shift" promoted to a real numbered chapter with a discovery-progression chip row, Services intro paragraph, Transformation restructured to 5 layers, Approach gained an "Organize" step + a grounded "give humans their time back" closer, Plans gained a Who-We-Help strip + MV Impact/MV Guidance renames
- [x] New closing passage (`MissionBeat` in `shared.tsx`) between Plans and Contact — the one explicit statement of the "Make India AI-ready" mission, paired with a hedged "future is arriving quietly" line
- [x] Full chapter renumbering 01-11 across every `ChapterEyebrow` call site
- [x] Image placeholder system: `AnnotatedImageReveal`'s `src` made optional with a placeholder-fill fallback; new `ImagePlaceholder` primitive for compact slots — see Open Questions for the 2 `IMAGE REQUIRED` specs
- [x] **Bug found and fixed along the way**: `StoryThread`'s `if (reduce) return null` (existing code, predates this milestone) caused a real hydration mismatch for any visitor with OS-level reduced-motion enabled — SSR always renders it (no `window`), but the client's first hydration pass immediately reads the true media query and unmounts it, producing a structural tree mismatch React has to discard and re-render. Never caught before because no prior verification pass tested with `reducedMotion: 'reduce'`. Fixed with a mount-gated state update instead of a direct render-time branch.
- [x] Verified with a production build + Playwright: desktop (1440px) and mobile (375px), light + dark, `scrollWidth === clientWidth` on all four, zero console errors, and a dedicated reduced-motion pass (clean after the fix above)

# Phase 2 — Lead Generation & Automation
*(Not started — do not build ahead of Phase 1.)* WhatsApp, email workflows, CRM/lead management, AI assistant, analytics, follow-up automation.

# Phase 3 — Studio Operations
*(Not started.)* Confirmed status as of 2026-09-03: no legal entity, no GST, no trademark filed — Studio currently operates as an individual/founder.
- [ ] Legal structure decision (sole proprietorship / LLP / Pvt Ltd) — flag for professional advice, don't give legal advice as fact
- [ ] Company registration + GST (if applicable)
- [ ] Trademark filing for "MVSingh Studio" name/mark
- [ ] Business banking
- [ ] Contracts, privacy policy, terms of service, service agreements
- [ ] Data protection compliance review
- [ ] Client systems, scalable delivery process

# Phase 4 — Products
*(Not started.)* Business management software, gym/yoga/healthcare software, CMS/CRM products, Digital Gurukul, Sehat-Saathi successor.
