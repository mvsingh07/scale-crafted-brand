import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact — Manvir Singh (MV Singh)",
  description: "Get in touch with Manvir Singh (MV Singh) — software engineer and AI strategist.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
