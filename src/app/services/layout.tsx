import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services — Manvir Singh (MV Singh)",
  description: "Backend architecture, scalable microservices, real-time systems, and AI integration services from Manvir Singh (MV Singh).",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
